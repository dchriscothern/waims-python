# WAIMS — Python requirements
# Generated from full import audit across all project files
# Install: pip install -r requirements.txt

# Core data
pandas>=2.0
numpy>=1.24

# ML
scikit-learn>=1.3

# Visualization
plotly>=5.18
streamlit>=1.32

# Database (built-in, listed for clarity)
# sqlite3 — standard library, no install needed

# HTTP / API (for research_monitor.py PubMed + RSS)
requests>=2.31
feedparser>=6.0

# Stats (correlation_explorer, z_score_module, model_validation)
scipy>=1.11

# PDF generation (WAIMS_Coach_Overview.pdf, WAIMS_SportScientist_Overview.pdf)
reportlab>=4.0

# Word document generation (WAIMS_Roadmap_2026.docx, demo cheat sheet)
# Install via npm: npm install -g docx
# docx is a Node.js package — not pip

# Optional — only needed if statsmodels was not fully removed
# statsmodels>=0.14

# Dev / testing
pytest>=7.0
